.design-import/
node_modules/
.next/
next-env.d.ts
*.tsbuildinfo
out/

# Secrets — never commit. .env.local holds the Cloudflare API token locally;
# a stray `git add .` here would leak it to a PUBLIC repo.
.env*

# Visual-diff captures live in the session scratchpad, not the repo.
.playwright-mcp/
